package com.uhufor.inspector.config

import com.uhufor.inspector.TraverseType
import com.uhufor.inspector.UnitMode

class Config {
    var unitMode: UnitMode = UnitMode.DP
    var traverseType: TraverseType = TraverseType.HIERARCHICAL
}
